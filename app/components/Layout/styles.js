import { StyleSheet } from "react-native";

import colors from '../../config/colors';
import global from '../../config/global';

const styles = StyleSheet.create({
    content : {
        padding: global.screenPad,
        flexDirection: "column",
        flex:1
    }
})

export default styles;